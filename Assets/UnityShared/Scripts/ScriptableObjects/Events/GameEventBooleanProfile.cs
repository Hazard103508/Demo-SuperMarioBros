using UnityEngine;

namespace UnityShared.ScriptableObjects.Events
{
    [CreateAssetMenu(fileName = "GameEventBooleanProfile", menuName = "ScriptableObjects/Events/Game Event (bool)", order = 6)]
    public class GameEventBooleanProfile : GameEventGenericProfile<bool>
    {
    }
}