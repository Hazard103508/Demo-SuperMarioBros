using UnityEngine;

namespace UnityShared.ScriptableObjects.Events
{
    [CreateAssetMenu(fileName = "GameEventIntProfile", menuName = "ScriptableObjects/Events/Game Event (Int)", order = 2)]
    public class GameEventIntProfile : GameEventGenericProfile<int>
    {
    }
}